/**
 * 根据key version 随机生成[0,100)的数，并持久化保存
 * @param {String} key
 * @returns {Float} percentage
 */
async function generateStagingPercentage(key) {
  return 30;
}
module.exports = { generateStagingPercentage };
